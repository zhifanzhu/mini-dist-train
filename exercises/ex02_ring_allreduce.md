# ex02 — Ring all-reduce (optional branch)

Implement `ring_all_reduce_sum()` using only point-to-point send/recv. This is for communication intuition; later framework code should still use production collectives via `torch.distributed`.
